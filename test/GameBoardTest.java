/*import org.junit.*;
*/
/**
 * Created by geoff on 3/8/17.
 */
/*
public class GameBoardTest {
    //some testing stuff should happen in here, for sure

    @Test
    public void createGameBoardTest(){
        GameBoard board = new GameBoard();

        Assert.assertTrue(board instanceof GameBoard);
    }

    @Test
    public void placingFirstTileTest() {
        Hex one = new Hex(0,0, terrainTypes.JUNGLE);
        Hex two = new Hex(1, 0, terrainTypes.LAKE);
        Hex three = new Hex(2,0,terrainTypes.VOLCANO);

        Tile tile = new Tile(one,two,three);


        GameBoard gameBoard = new GameBoard();
        gameBoard.receiveFirstTile(tile);

        boolean flag = true;

        /*for(int i = 0; i<19 ; i++){
            for(int j = 0; j<19 ; j++){
                if(gameBoard.getBoard()[i][j] == null){
                    System.out.print("N");
                }else{
                    System.out.print("Y");
                }
            }
            System.out.println();
        }*/
/*
        System.out.println(gameBoard.getBoard()[10][10].getHexTerrainType());
        if(gameBoard.getBoard()[10][10].getHexTerrainType() != terrainTypes.JUNGLE){
            flag = false;
        }

        System.out.println(gameBoard.getBoard()[12][10].getHexTerrainType());
        if(gameBoard.getBoard()[12][10].getHexTerrainType() != terrainTypes.LAKE){
            flag = false;
        }

        System.out.println(gameBoard.getBoard()[11][9].getHexTerrainType());
        if(gameBoard.getBoard()[11][9].getHexTerrainType() != terrainTypes.VOLCANO){
            flag = false;
        }

        Assert.assertTrue(flag);
    }
}*/
